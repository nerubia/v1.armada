import { ConnectionOpts } from '../middlewares/database'

export const database: ConnectionOpts = {
  database: 'codefantry_test',
  host: 'localhost',
  password: 'testerpass',
  port: 5433,
  user: 'tester',
}
